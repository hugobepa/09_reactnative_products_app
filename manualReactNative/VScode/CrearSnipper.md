1. selecionar el metodo que se quiere crear
2. ir command palete -- easy snipper:add
3. añadir nombre añafir funcion
4. modificar funcion cambiar nombres por " ${1:label} " para poder escribi directamente despues
5. por "snippet manager" ver y cambiar snippet creados
6. guardar y probar

#export const useLocationStore = create<LocationState>()((set, get) => ({\n\t}));

import { create } from 'zustand'

export const nameConstante = create<tipo>()((set, get) => ({}));
(https://www.youtube.com/watch?v=DaqK691TyF0)
export const ${1:label} = create<${2:label}>()((set, get) => ({\n \t}));
